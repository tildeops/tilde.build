/**
 * Email mockups use the shared PhoneFrame so every iPhone mockup on the site
 * reads as the same device. Re-exported here to keep the local import path.
 */
export { PhoneFrame, PHONE_FRAME_WIDTH } from "@/components/ui/phone-frame";
